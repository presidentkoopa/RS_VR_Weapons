// NO BOM IN THIS FILE: a UTF-8 byte order mark makes the engine read the first
// alias name with the mark glued to its front, and the alias silently does
// nothing (RS_ModelSwapper lost a release to exactly that).

// Both shotguns and 50 shells, each shotgun put straight into its own hand.
alias wm_giveshotguns "netevent wm_giveshotguns"

// This package's two test pistols back into their hands, if carried.
alias wm_equippistols "netevent wm_equippistols"

// Moonlight into the main hand, Sunset into the off hand, and 60 rounds.
alias wm_giverevolvers "netevent wm_giverevolvers"

// The Cola revolver into the off hand.
alias wm_givecola "netevent wm_givecola"

// The Rifle into the main hand, and 60 rounds.
alias wm_giverifle "netevent wm_giverifle"

// The super shotgun into the main hand, and 20 shells.
alias wm_givessg "netevent wm_givessg"

// The double barrel into the off hand, and 20 shells.
alias wm_givedoublebarrel "netevent wm_givedoublebarrel"

// The M16 into the main hand, and 60 rounds.
alias wm_givem16 "netevent wm_givem16"

// The Tec9 into the off hand, and 64 rounds.
alias wm_givetec9 "netevent wm_givetec9"

// The SMG into the main hand, and 60 rounds.
alias wm_givesmg "netevent wm_givesmg"

// Plasma rifle and plasma carbine into your hands (and 100 cells).
alias wm_giveplasma "netevent wm_giveplasma"

// Railgun into the main hand (and 100 cells).
alias wm_giverailgun "netevent wm_giverailgun"

// Chaingun into the main hand (and 100 rounds).
alias wm_givechaingun "netevent wm_givechaingun"

// Rocket launcher and RPG into your hands (and 12 rockets).
alias wm_givelaunchers "netevent wm_givelaunchers"

// BFG and heavy BFG into your hands (and 320 cells).
alias wm_givebfg "netevent wm_givebfg"

// Chainsaw and heavy chainsaw into your hands.
alias wm_givechainsaws "netevent wm_givechainsaws"

// Flamer and flamethrower into your hands (and 200 cells).
alias wm_giveflamers "netevent wm_giveflamers"

// Machine gun into the off hand (and 100 rounds, 6 grenades with RS Grenade).
alias wm_givemachinegun "netevent wm_givemachinegun"
alias wm_giveassaultshotguns "netevent wm_giveassaultshotguns"
alias wm_givebolter "netevent wm_givebolter"
alias wm_givebfgrifle "netevent wm_givebfgrifle"
alias wm_giverotaries "netevent wm_giverotaries"
alias wm_givelongbar "netevent wm_givelongbar"
alias wm_giveunmaker "netevent wm_giveunmaker"

// RS Grenade's grenade into the main hand (and 10 grenades), when RS_Grenade is loaded.
alias wm_givegrenade "netevent wm_givegrenade"

// The ShieldSaw onto your forearm, when RS_ShieldSaw is loaded -- its own draw takes it from there.
alias wm_giveshieldsaw "netevent wm_giveshieldsaw"

// ================================================================================================
// RS_SHIELDSAW'S KEYCONF, folded in 09-14 from presidentkoopa/RS_ShieldSaw 4bb43ad -- copied whole, every name unchanged so saved values and bindings carry over. Loading RS_ShieldSaw.pk3 as well is a startup error.
// ================================================================================================

// addslotdefault APPENDS; setslot would clear the slot and take Fist and
// Chainsaw away from every other mod in the load order.
addslotdefault 1 RS_ShieldSaw

// THE DRAW. Pulls the shield off your forearm into your off hand, setting your
// current off-hand weapon aside; pressed again it puts it back. This is a KEY
// today because it wants to be an ARM GESTURE and that needs hand velocity
// from the engine. The binding stays either way -- a gesture should not be the
// only way to reach a weapon.
alias rs_shieldsaw_draw "netevent rs-ss-toggle"
defaultbind X rs_shieldsaw_draw

// addmenukey needs a section to hang under; without addkeysection first the
// engine refuses it with "You must use addkeysection first."
addkeysection "RS Shield Saw" rs_shieldsaw
addmenukey "$RS_SS_DRAW" rs_shieldsaw_draw
