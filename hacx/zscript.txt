version "5.0.0"

// RS_VR_Weapons_HacX -- the HacX SET, an add-on to RS_VR_Weapons.pk3.
//
// It carries its own meshes and cards and nothing else: every shared class is the base's, and
// defining one twice is a fatal, global load error the moment both archives load.
//
// LOAD IT AFTER RS_VR_Weapons.pk3 -- every class here derives from the base's WM_Gun, WM_Prop or
// WM_SetBridge, and ZScript resolves classes in load order.

#include "zscript/rs_vr_weapons_hacx/props_hacx.zs"
#include "zscript/rs_vr_weapons_hacx/generated_guns_hacx.zs"   // the gun class writer's
#include "zscript/rs_vr_weapons_hacx/loadout_hacx.zs"
#include "zscript/rs_vr_weapons_hacx/bridge_hacx.zs"
