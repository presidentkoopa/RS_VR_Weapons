version "5.0.0"

// RS_VR_Weapons_Cola -- the Cola SET, an add-on to RS_VR_Weapons.pk3.
//
// It carries its own meshes, cards and sounds and nothing else: every shared class is the base's,
// and defining one twice is a fatal, global load error the moment both archives load.
//
// LOAD IT AFTER RS_VR_Weapons.pk3 -- every class here derives from the base's WM_Gun, WM_Prop or
// WM_SetBridge, and ZScript resolves classes in load order.

#include "zscript/rs_vr_weapons_cola/micromissile.zs"   // the Sidewinder's round
#include "zscript/rs_vr_weapons_cola/props_cola.zs"
#include "zscript/rs_vr_weapons_cola/generated_guns_cola.zs"   // the gun class writer's
#include "zscript/rs_vr_weapons_cola/loadout_cola.zs"
#include "zscript/rs_vr_weapons_cola/bridge_cola.zs"
