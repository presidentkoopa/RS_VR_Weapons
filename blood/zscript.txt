version "5.0.0"

// RS_VR_Weapons_Blood -- the Blood SET, an add-on to RS_VR_Weapons.pk3.
//
// It carries its own meshes and cards and nothing else: every shared class is the base's, and
// defining one twice is a fatal, global load error the moment both archives load.
//
// LOAD IT AFTER RS_VR_Weapons.pk3 -- every class here derives from the base's WM_Gun, WM_Prop or
// WM_SetBridge, and ZScript resolves classes in load order.

#include "zscript/rs_vr_weapons_blood/spraycan.zs"          // a flame gun, not a plain gun
#include "zscript/rs_vr_weapons_blood/napalmrocket.zs"      // the napalm shot, so it can name its own pool
#include "zscript/rs_vr_weapons_blood/thrownlighter.zs"   // what BL_Lighter becomes in flight
#include "zscript/rs_vr_weapons_blood/props_blood.zs"
#include "zscript/rs_vr_weapons_blood/generated_guns_blood.zs"   // the gun class writer's
#include "zscript/rs_vr_weapons_blood/loadout_blood.zs"
#include "zscript/rs_vr_weapons_blood/bridge_blood.zs"
